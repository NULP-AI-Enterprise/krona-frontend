apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: krona-frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/NULP-AI-Enterprise/krona-frontend
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: krona
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
